server:
  port: 8086

spring:
  application:
    name: ms-notificaciones
  datasource:
    url: jdbc:mysql://localhost:3306/foro_notificaciones_db
    username: root
    password: 
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/