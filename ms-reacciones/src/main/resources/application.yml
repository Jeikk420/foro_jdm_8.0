server:
  port: 8087

spring:
  application:
    name: ms-reacciones
  datasource:
    url: jdbc:mysql://localhost:3306/foro_reacciones_db
    username: root
    password: 
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/